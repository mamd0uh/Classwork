module.exports = {
    area (radius){
    return radius*radius*(Math.PI)
},
    circumference (radius){
    return radius*2*Math.PI
}
}

// function area(r){
//     return (Math.PIrr).toFixed(2)
   
//    }
//    function circumference(r){
//    return (2Math.PIr).toFixed(2)
//    }
   
//    module.exports = {area,circumference}